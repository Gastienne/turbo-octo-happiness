name: End-to-end tests
on:
  pull_request_review:
    types: [submitted]
    branches: ['master']
    paths: [' **path/**']
  workflow_dispatch: ~
jobs:
  cypress-run:
    if: ${{ github.event.pull_request.requested_reviewers | jq '. | length > 2}}
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      # Install NPM dependencies, cache them correctly
      # and run all Cypress tests
      - name: Setup node 14
        uses: actions/setup-node@v2
        with:
          node-version: 14.x
      - run: yarn install
      - run: yarn test:e2e:ci
